// Draft media primitives for adapter parity discussions.
// These are aliases to the canonical adapter types to avoid duplication.

export type {
  ImageCall,
  ImageModel,
  ImageResult,
  SpeechCall,
  SpeechModel,
  SpeechResult,
  TranscriptionCall,
  TranscriptionModel,
  TranscriptionResult,
  TranscriptionSegment,
} from "../types";
