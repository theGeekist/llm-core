#!/usr/bin/env sh
set -eu

# Compatibility hook path for checkouts that explicitly use .githooks.
# lint-staged preserves unstaged hunks and reapplies successful fixes to the index.
exec bun run lint:staged
