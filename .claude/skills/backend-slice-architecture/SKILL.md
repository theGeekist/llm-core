---
name: backend-slice-architecture
description: Use when creating, refactoring, reviewing, or incrementally migrating backend architecture, including existing monorepos, to enforce a feature-first package-shaped model with explicit feature public APIs, dependency direction, composition roots, and migration boundaries.
---

# Backend Slice Architecture

This project skill intentionally delegates to the same canonical skill used by
Codex so the two coding environments cannot drift.

Before inspecting, reviewing, designing, planning, or editing backend
architecture:

1. Read
   `/Users/jasonnathan/.codex/skills/backend-slice-architecture/SKILL.md`
   completely.
2. Follow every applicable instruction in that skill as if its contents were
   present in this file.
3. Read any reference that canonical skill requires for the current task.
4. Apply the repository's `AGENTS.md`, `CLAUDE.md`, accepted ADRs, task brief,
   and declared write scope as additional constraints.

If the canonical skill cannot be read, stop architecture work and report the
missing dependency instead of substituting a different architecture model.
