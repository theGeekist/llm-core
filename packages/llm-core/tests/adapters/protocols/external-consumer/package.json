{
  "name": "@geekist/llm-core-protocol-external-consumer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:packed": "bun run prepare-packed.ts",
    "prepare:packed:prebuilt": "bun run prepare-packed.ts --prebuilt",
    "qualify": "bun run prepare:packed && bun run verify:a2a && bun run verify:mcp",
    "qualify:prebuilt": "bun run prepare:packed:prebuilt && bun run verify:a2a && bun run verify:mcp",
    "qualify:a2a": "bun run prepare:packed && bun run verify:a2a",
    "qualify:mcp": "bun run prepare:packed && bun run verify:mcp",
    "verify:a2a": "bun run qualify-a2a.ts",
    "verify:mcp": "bun run qualify-mcp.ts"
  },
  "dependencies": {
    "@a2a-js/sdk": "1.0.0",
    "@modelcontextprotocol/client": "2.0.0",
    "@modelcontextprotocol/server": "2.0.0",
    "@wpkernel/pipeline": "1.2.1"
  }
}
