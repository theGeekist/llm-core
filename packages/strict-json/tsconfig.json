{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "types": ["bun", "node"],
    "paths": {
      "@geekist/strict-json": ["./index.ts"]
    }
  },
  "include": ["index.ts", "src", "tests", "scripts"],
  "exclude": ["node_modules", "dist", "coverage"]
}
